# Wave E4 — body.tex cross-reference updates required (supplement renumbering)

**Owner of this change:** the agent that owns `paper_draft/latex/body.tex`.
**Reason:** Wave E4 made the supplement's figures/tables *continue* the main
document's numbering (IEEE SPS Information for Authors), instead of the old
`S`-prefixed scheme. In `supplement.tex` the supplementary **figures now render
7–12** and the supplementary **tables now render II–III** (IEEEtran renders
tables in Roman; they continue the main document's `Table I`). Supplementary
**section** labels are unchanged (still `S1`–`S5`).

`body.tex` points into the supplement with **hard-coded text strings** (not
`\ref`), so those strings are now stale and must be updated by hand. The
supplement's own internal `\ref`s already resolve to the new numbers and needed
no manual edit.

## 1. Figure references — additive remap `S k → k+6`

| Old string | New string | body.tex lines (occurrences) |
|---|---|---|
| `Fig.~S1` | `Fig.~7`  | 316 (×1) |
| `Fig.~S2` | `Fig.~8`  | 203 (×1), 209 (×1), 302 (×2), 332 (×1) |
| `Fig.~S3` | `Fig.~9`  | 233 (×4), 238 (×2), 259 (×1), 300 (×2), 348 (×1) |
| `Fig.~S4` | `Fig.~10` | 318 (×1) |
| `Fig.~S5` | `Fig.~11` | 320 (×1) |
| `Fig.~S6` | `Fig.~12` | 304 (×1) |

Total: **19 figure-reference occurrences**. The remap is purely additive
(supplement figure S*k* → main-continued figure *k*+6), so every occurrence of a
given `Fig.~S#` maps the same way regardless of context. A global
find/replace of each `Fig.~S#` string is safe.

Content cross-check (label → what it is, so you can sanity-check):
`Fig.~7` = permutation-whitening / factorized ablation; `Fig.~8` = acquisition-
time ablation; `Fig.~9` = reconstruction bridge; `Fig.~10` = coherent-residual
(E4); `Fig.~11` = C0 audit + oracle baselines; `Fig.~12` = flip-boundary detail
+ low-photon floor probe.

> **Pre-existing typo to consider (not caused by E4):** body.tex line 238 reads
> "used throughout `Fig.~S3` and `Fig.~S3`" — the same figure twice. Mechanical
> remap makes it "Fig.~9 and Fig.~9". You may have intended "Fig.~9 and Fig.~10"
> (bridge + coherent-residual). Your call; E4 only did the mechanical remap here.

## 2. Table references — `S1 → II`, `S2 → III` (see numeral note below)

| Old string | New string (recommended) | body.tex lines (occurrences) |
|---|---|---|
| `Table~S1` | `Table~II`  | 265 (×1, inside the Table-1 `\caption`) |
| `Table~S2` | `Table~III` | 162 (×1, inside the Fig-3 `\caption`), 296 (×2), 298 (×1) |

Total: **5 table-reference occurrences.**

**Numeral convention (please decide):** IEEEtran numbers tables in **Roman**, so
the supplement's two tables render as `TABLE II` and `TABLE III`, continuing the
main document's `TABLE I`. To match what the reader actually sees, the
recommended replacements above are Roman (`Table~II`, `Table~III`).

- Note the *pre-existing* inconsistency in body.tex: the **main** table is
  referred to in prose as `Table~1` (arabic, e.g. line 261 and the start of the
  line-265 caption) even though it renders as `TABLE I`. E4 did **not** touch
  that.
- If you prefer **arabic** table numerals everywhere (`Table 1/2/3`, matching
  your existing arabic prose and the figure style), that requires adding
  `\renewcommand{\thetable}{\arabic{table}}` to **both** `main.tex` **and**
  `supplement.tex` — coordinate with the E4 agent, as those are E4-owned files.
  With that change, use `Table~S1 → Table~2` and `Table~S2 → Table~3`. E4 did
  not apply it, to preserve IEEEtran's default main-document table style.

## 3. Do NOT change — supplementary *section* pointers (still `S`-prefixed)

Supplementary sections keep the `S` prefix. Leave these strings **as-is**:
`Supplementary Material S1`, `…S2`, `…S3`, `…S4`, `…S5`, and the range
`Supplementary Material S1--S4` (line 312), plus the prose enumeration
`S1 --- … S5 ---` in the `\section*{Supplementary Material}` summary (line 348).
Occurrences of bare `S1`…`S5` that are *section* pointers (all phrased
"Supplementary Material S#") must NOT be remapped. Only `Fig.~S#` and
`Table~S#` change.

*(Generated by Wave E4. After applying, recompile main.tex; there are no `\ref`
dependencies to break since these were literal strings.)*
