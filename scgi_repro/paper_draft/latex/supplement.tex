\documentclass[journal,twoside]{IEEEtran}
% Supplementary Material for the identifiability manuscript (IEEE-TCI target).
% Standalone document: sections S1--S5 (supplementary experiments + protocol) followed by
% Appendices A--F (full proofs, moved out of main.tex for the 13-page limit).
% Shared preamble macros live in macros.tex; the reference list
% (bibliography_supp.tex) repeats the main document's entries [1]--[28] in the
% same order, so citation numbers agree across both PDFs.

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsfonts,mathtools}
\usepackage{bm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
\usepackage{xcolor}
\usepackage{float}

\hypersetup{
  pdftitle={Supplementary Material for: Identifiability by Stationarity: Blind Gain-Object Separation in Self-Calibrating Ghost Imaging},
  pdfauthor={CHEN Yongzi},
  pdfsubject={Supplementary sections S1-S5 and Appendices A-F},
  pdfkeywords={ghost imaging, single-pixel imaging, blind calibration, identifiability, bilinear inverse problems},
  hypertexnames=false,
}

\input{macros}

% Supplement numbering (IEEE SPS Information for Authors): supplementary
% SECTIONS keep the S-prefix (S1..S5), but FIGURES and TABLES CONTINUE the main
% document's counters (main ends at Fig. 6 and Table 1). The figure/table
% counters are advanced immediately after \maketitle below, so the first
% supplementary figure is Fig. 7 and the first supplementary table is Table 2.
% Equations: the main text uses no numbered display equations, and the
% appendices carry their own per-appendix tags (A.x..F.x) via \appendices, so
% no global equation-counter offset is required.
\renewcommand{\thesection}{S\arabic{section}}

\begin{document}

\title{Supplementary Material for: Identifiability by Stationarity: Blind Gain--Object Separation in Self-Calibrating Ghost Imaging}

\author{%
CHEN Yongzi\thanks{Draft rendering; author list and affiliations to be finalized before submission. Extended supplementary material; length subject to editorial approval.}%
}

\markboth{Supplementary material --- draft (rendered \today)}{Identifiability by stationarity: supplementary material}

\maketitle

% Continue the main document's float numbering (main: Figs. 1--6, Table 1),
% so supplementary figures run 7--12 and supplementary tables run 2--3.
\setcounter{figure}{6}
\setcounter{table}{1}

\begin{abstract}
This document supplements the main manuscript. Sections~S1--S5 report the supplementary verification experiments promised in Sec.~9 of the main text: the permutation-whitening / factorized-randomization ablation (S1), the coherent-residual validation of the master identity (S2), the $C_0$ audit and the oracle-to-blind baselines (S3), and the extended flip-boundary detail with the low-photon drift-floor probe and replication tables (S4), and the shared simulation protocol (S5). Appendices~A--F contain the complete proofs of every theorem, corollary, and proposition quoted in the main text. Citation numbers, section numbers (Sec.~1--10), figure numbers (Fig.~1--6), theorem labels, and equation tags (e.g.\ (F.10)) referring to the main manuscript retain their main-text meaning; figures and tables introduced here \emph{continue} the main numbering (Figs.~7--12, Tables~2--3), and only the supplementary \emph{sections} keep an S-prefix (S1--S5). All numerical values are descriptive results of the specified simulation grids unless a resampled interval is stated.
\end{abstract}

\section{Permutation whitening and the factorized randomization ablation}
\label{sec:s1}

This section carries the multi-permutation whitening-power experiment and the factorized randomization ablation summarized in Sec.~9.6 of the main text (data: \texttt{results/perm\_ablation\_r1/}; 32 draws per randomized arm $\times$ 10 objects under the Fig.~2 protocol --- $K=4096$, $64\times64$ objects, 8192 paired frames, noiseless carrier, Brown--Forsythe/Levene screen \cite{BrownForsythe1974,Levene1960} at 8 chunks, reject at $p<10^{-3}$). Two randomization semantics must be kept separate (Appendix~E.6): the \emph{pixel-level} randomization of the SRHT theory --- pixel permutation $P_{\mathrm{col}}$ and pixel signs $D$, whose Lemma~E.1 covariance is invariant to acquisition order --- and the \emph{acquisition-order} reordering $P_{\mathrm{row}}$, which controls temporal stationarity of the acquired series. Figure~\ref{fig:s1}(a) reports the factorized attribution: either permutation alone substantially reduces the temporal-stationarity screen's rejection rate --- from $70\%$ (ordered) to $5.6\%$ ($P_{\mathrm{col}}$) and $12.2\%$ ($P_{\mathrm{row}}$), all still far above the $\sim0.1\%$ nominal rate of the $p<10^{-3}$ test --- the theory's own randomization $P_{\mathrm{col}}{+}D$ reduces it to $11.9\%$, the code's SRHT arm ($P_{\mathrm{row}}{+}D$) to $5.9\%$, and the historical combined arm ($P_{\mathrm{col}}{+}P_{\mathrm{row}}$) reproduces its $19/320=5.9\%$ headline --- whereas pixel signs $D$ alone barely move it ($43.4\%$), because sign flips whiten carrier amplitudes but leave the coarse-to-fine sequency-energy ordering of natural-Hadamard rows intact. Figure~\ref{fig:s1}(b) shows the honest per-draw certificate null quoted in the main text: on the historical arm, the maximum non-DC Walsh coefficient of the permuted $T^2$ does not predict which draws reject (Spearman $0.092$, $p=0.10$), consistent with the probabilistic --- not per-draw-certifiable --- nature of permutation whitening (Sec.~7).

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figS1_perm_whitening.png}
\caption{Permutation whitening and factorized randomization ablation (32 draws per randomized arm $\times$ 10 objects; Levene screen at $p<10^{-3}$). (a) Rejection rate per arm: either permutation alone substantially reduces the rejection rate ($70\%\to5.6$--$12.2\%$, all $\gg$ the $\sim0.1\%$ nominal rate); pixel signs alone barely move it; outlined bars mark the theory's randomization ($P_{\mathrm{col}}{+}D$) and the code's SRHT arm ($P_{\mathrm{row}}{+}D$). (b) Per-draw certificate null on the historical $P_{\mathrm{col}}{+}P_{\mathrm{row}}$ arm (320 draws): max non-DC $|\widehat{w^P}|/S_2$ of the permuted squared object versus the Levene $p$-value (violet: accepted draws; red: rejected; dashed line: the $p=10^{-3}$ gate). Walsh flatness of the realized draw does not predict rejection (Spearman $0.092$, $p=0.10$).}
\label{fig:s1}
\end{figure}

The acquisition-time ablation of the main text's Sec.~9.4 (moved here from the main document for space) is reproduced as Fig.~\ref{fig:s1b}: at the single above-floor drift point $\rho=10^{-3}$, acquisition-order permutation alone gains $+5.39$~dB over ordered Hadamard, pixel sign flips alone $+5.47$~dB, and the code's full SRHT arm $+5.45$~dB --- each ingredient by itself recovers essentially the full advantage, and combining them is not additive. All Fig.~\ref{fig:s1b} arms randomize acquisition time only; the pixel-permutation ($P_{\mathrm{col}}$) attribution is the subject of Fig.~\ref{fig:s1} above.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig6_ablation.png}
\caption{Ablation isolating the acquisition-time randomization mechanism (moved from the main document). (a) PSNR gain over ordered Hadamard at the single above-floor drift point $\rho=10^{-3}$ for five arms: acquisition-order (row) permutation $P_{\mathrm{row}}$ alone gains $+5.39$~dB, pixel sign flips $D$ alone $+5.47$~dB, and the code's full SRHT arm ($P_{\mathrm{row}}{+}D$) $+5.45$~dB, with the block-shuffle and time-interleave sign variants in the same $+5.5$~dB band --- each ingredient by itself recovers essentially the full advantage, and combining them is not additive. All five arms randomize acquisition time only: none applies the pixel/column permutation $P_{\mathrm{col}}$ of the Appendix~E SRHT analysis, whose factorized attribution is Fig.~\ref{fig:s1}. (b) The full-SRHT advantage versus drift rate $\rho$: the gain collapses toward $0$~dB as every blind variant approaches the noise floor (shaded: sub-floor cells). What matters is that the chronology is randomized, not the particular form of the randomization. Descriptive values at single grid points (no resampled intervals).}
\label{fig:s1b}
\end{figure}

\section{Validation of the master identity: reconstruction bridge and coherent residuals (E4)}
\label{sec:s2}

This section carries the two validations of Theorem~1. Figure~\ref{fig:s2bridge} (moved from the main document for space) shows the reconstruction-bridge test of main-text Sec.~9.3: predicted versus measured scale-aligned relMSE on the balanced 8100-row design; the quantitative slopes and leverage values are in main-text Sec.~9.3. The remainder of the section carries experiment E4: the direct, raw-metric validation of Theorem~1 under \emph{injected} (exogenous) coherent residuals, the regime in which hypotheses (H1)--(H3) hold by construction.

\begin{figure}[t]
\centering
\includegraphics[width=0.9\columnwidth]{fig4_relmse_theory_vs_sim.png}
\caption{Reconstruction bridge (Theorem~1 tested as an identity, on a balanced 8100-row design: three reconstructors, $N\in\{1024,2048,4096\}$, six injected residual-gain variances $v$, ten objects, fifteen seeds; moved from the main document). Predicted versus measured scale-aligned relMSE: the deterministic inverses are flat in $N$, as $B_L=1$ demands, while the random/DGI arm decays as $1/N$ at every $v$ with a quantitative leverage law. Slopes, leverage values, and the DC-dominated alignment effect: main-text Sec.~9.3.}
\label{fig:s2bridge}
\end{figure} Five residual-covariance structures --- i.i.d., AR(1), sinusoidal, low-pass OU, blockwise --- are injected at fixed $v=10^{-3}$ across three reconstructors and ten objects (150 cells, $K=N=1024$; data: \texttt{results/coherent\_residual\_e4\_r1/}), and the measurement is made in the raw-residual (identity-verification) metric rather than the scale-aligned image metric of the bridge experiment (Fig.~\ref{fig:s2bridge}). Figure~\ref{fig:s2}(a) shows the full matrix law of Theorem~1 tracking the measured relMSE within $4.2\%$ across all 150 cells. Figure~\ref{fig:s2}(b) isolates the coherent clause: the scalar $v\,B_L$ shortcut is exact for the complete orthogonal and SRHT inversions (for which $LL^{\top}\propto I/N$ analytically, so the gain term feels only $\operatorname{diag}(V_\delta)$) but fails by up to $3.65\times$ for random/DGI under low-pass-coherent residuals --- exactly the failure mode Theorem~1's full matrix form exists to capture. E4 validates the matrix law under injected residuals; it does not probe the same-record $\delta$--$\xi$ dependence flagged after Theorem~1 in the main text.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figS2_coherent_residual.png}
\caption{Coherent-residual validation of Theorem~1 (E4; five injected residual structures $\times$ three reconstructors $\times$ ten objects at $v=10^{-3}$, raw identity-verification metric). (a) Matrix-law prediction versus measured relMSE: agreement within $4.2\%$ across all 150 cells. (b) Ratio of measured relMSE to the scalar $v\,B_L$ law by residual structure: exact for orthogonal and SRHT inversions, but up to $3.65\times$ off for random/DGI under low-pass OU residuals --- a direct verification of Theorem~1's coherent (full-matrix) clause.}
\label{fig:s2}
\end{figure}

\section{\texorpdfstring{$C_0$}{C0} audit and oracle-to-blind baselines}
\label{sec:s3}

\emph{$C_0$ audit (E9).} The one-sample DGI correlator constant $C_0=\mathbb{E}\|Z\|^2/S_2-1$ is measured from $2\times10^{4}$ single-frame pattern draws per ensemble, for four pattern ensembles $\times$ five objects (data: \texttt{results/c0\_audit\_e9\_r1/}). Figure~\ref{fig:s3}(a) compares the measurement with the moment formula of Appendix~F: mean relative error $0.35\%$, worst $2.01\%$, across values of $C_0$ spanning $10^{3}$ to $5.7\times10^{6}$. The strong ensemble dependence --- $O(K)$ for signed Rademacher patterns versus $O(K_{\mathrm{eff}}K)$ for the nonnegative offset ensembles --- confirms the main text's bookkeeping rule that $C_0$ is pipeline-specific, as Theorem~1's DGI specialization insists: there is no universal correlator constant to tabulate.

\emph{Oracle, metered, and blind baselines (E11).} On four held-out objects with OU drift ($\rho=10^{-3}$, $s=0.1$; five gain treatments $\times$ five seeds; data: \texttt{results/oracle\_baselines\_e11\_r1/}), Fig.~\ref{fig:s3}(b) shows the accountability ordering static $\ge$ oracle $\ge$ blind-AGC $\ge$ no-correction holding in CNR for 4 of 4 objects, with an oracle$-$blind CNR gap of only $0.035$--$0.147$: at this drift point the blind windowed estimator of Theorem~B concedes little to a gain oracle, while forgoing correction altogether costs the most. This supports the main text's reading that the practical price of blindness is small once the stationarity anchor is available.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figS3_c0_oracle.png}
\caption{$C_0$ audit and oracle-to-blind baselines. (a) Measured one-sample correlator constant $C_0$ versus the Appendix-F moment formula for four pattern ensembles $\times$ five objects ($2\times10^{4}$ draws each): mean relative error $0.35\%$ (worst $2.01\%$); the $O(K)$ Rademacher versus $O(K_{\mathrm{eff}}K)$ offset-ensemble spread confirms that $C_0$ is pipeline-specific. (b) GT-mask CNR under five gain treatments on four held-out objects (OU drift, $\rho=10^{-3}$, $s=0.1$; error bars: s.d.\ over five seeds): the ordering static $\ge$ oracle $\ge$ blind-AGC $\ge$ none holds for 4 of 4 objects, with oracle$-$blind gaps of $0.035$--$0.147$ CNR.}
\label{fig:s3}
\end{figure}

\section{Extended quantitative detail: flip boundary, low-photon floor, and replication tables}
\label{sec:s4}

This section collects the extended flip-boundary detail, the low-photon floor probe, and the per-arm replication values behind main-text Fig.~3 and Table~1.

\emph{Corrected-$C_0$ no-flip consistency.} Section~9.4 of the main text reports that, once the drift-leverage constant of the mean-subtracted blind random+DGI pipeline is corrected to the raw-correlator value $1+C_0^{\mathrm{raw}}$ of (F.10), Prop~3 predicts \emph{no flip} anywhere on the simulated grid --- and the data confirm it in 100/100 cells. Figure~\ref{fig:s4}(a) shows the per-cell margin behind that count (data: \texttt{results/prop3\_nofreeparam\_r1/}): for both blind corrections (AGC and SCGI proxy), the minimum over the drift grid of the flip criterion $Q-r$ stays strictly positive in every one of the $2\times50$ (correction $\times$ cell) checks, with the margin narrowing as $\sigma_a$ grows --- consistent with the boundary being pushed beyond the simulated grid rather than absent in principle.

\emph{Low-photon drift-limited floor.} Figure~\ref{fig:s4}(b) carries the floor probe behind the closing claim of Sec.~9's low-photon experiment (data: \texttt{results/paper\_fig7\_lowphoton\_r5\_final/}): at the high-photon end the log-domain gain-MSE floor is drift-limited rather than photon-limited --- the floor of $2.43\times10^{-4}$ at $\rho=10^{-3}$ falls to $1.41\times10^{-4}$ when the drift slows to $\rho=10^{-4}$ for the proxy arms, and from $2.17\times10^{-4}$ to $1.13\times10^{-4}$ for the oracle-known-carrier estimator of Theorem~C, whose floor sits $\sim11\%$ \emph{below} the proxies because it deconvolves the known carrier exactly; the arms coincide through the photon-limited part of the probed $\bar\lambda\ge4$ range and separate only at the floor.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figS4_flip_floorprobe.png}
\caption{Extended flip-boundary detail and the low-photon floor probe. (a) Corrected-$C_0$ no-flip margins for the blind random+DGI arm: per-cell minimum over the drift grid of $Q-r$ (the flip criterion of Prop~3 with drift-leverage constant $1+C_0^{\mathrm{raw}}$), for the AGC and SCGI-proxy corrections; a flip would require a negative value, and all 100/100 cells are positive, matching the no-flip outcome of Sec.~9.4. (b) High-photon floor probe: log-domain gain MSE versus mean photon budget $\bar\lambda$ for the uncalibrated proxy and the oracle-known-carrier calibrated soft-log (Theorem~C) at $\rho=10^{-3}$ (solid) and $\rho=10^{-4}$ (dashed); the two drift rates share the photon-limited regime and separate only at the floor ($2.43\times10^{-4}$ versus $1.41\times10^{-4}$ for the proxy, $2.17\times10^{-4}$ versus $1.13\times10^{-4}$ for the oracle-carrier arm, $\sim11\%$ lower), which is therefore drift-limited, not photon-limited.}
\label{fig:s4}
\end{figure}

Table~\ref{tab:s1} collects the secondary measured constants and grid conventions behind main-text Fig.~4 and Table~1 (moved here from the main document's Table~1 for space; the headline constants $C_0$, $D_H$, $v_{\mathrm{blind}}$, and the Prop~3 no-free-parameter agreement remain in the main text).

\begin{table*}[t]
\centering
\caption{Secondary measured constants and conventions behind main-text Fig.~4 (companion to main-text Table~1). Every constant is pipeline-specific; confidence conventions as in the main text (two-way seeds-and-objects crossed cluster bootstrap where stated).}
\label{tab:s1}
\begin{tabular}{@{}p{0.24\textwidth}p{0.49\textwidth}p{0.20\textwidth}@{}}
\toprule
Quantity & Value in this pipeline & Uncertainty \\
\midrule
$K_{\mathrm{eff}}$ (object-panel range) & $59.8$--$864.3$ & --- \\
\addlinespace
Drift / noise / flux conventions & OU log-gain, $\rho\in[10^{-3},10]$, drift s.d.\ $0.030$--$0.104$; noise $\sigma\in[0.05,0.5]$; equal frames and equal total flux per arm & --- \\
\addlinespace
Above-floor gate & $\mathrm{relMSE}<0.5$; $29$ of $45$ cells above floor (16 at noise floor) & cell count, not resampled; no bootstrap CI computed \\
\addlinespace
Empirical SRHT-paired vs.\ ordered-Hadamard crossover, two-parameter power-law fit & $R^2=0.989$--$0.9995$, fitted exponents $-0.94$ to $-1.93$ & descriptive fits of a comparison Prop~3 does not rank; survival-style CIs left to future work \\
\bottomrule
\end{tabular}
\end{table*}

\emph{Per-arm replication values (main-text Fig.~3 and Sec.~9.2).} Table~\ref{tab:s2} tabulates the best-window blind gain errors and the slow-drift variance-segment slopes per arm from the 20-seed uncertainty replication (fixed $W\le16$ variance-segment convention; 31{,}600 rows; medians with two-way seeds-and-objects crossed cluster bootstrap 95\% CIs), together with the raw-arm single-pass diagnostics and the exploratory 5-seed adaptive-segment slopes.

\begin{table*}[t]
\centering
\small
\caption{Per-arm best-window blind gain errors and $\log(\mathrm{err})$-vs-$\log W$ slopes behind main-text Fig.~3 ($\rho_{\mathrm{pair}}=10^{-3}$, $s=0.1$, 2048-frame budget; medians over 20 seeds $\times$ 10 objects with two-way clustered 95\% CIs; exploratory slopes from the earlier 5-seed adaptive-segment convention). The raw signed-Walsh arms are diagnostic-only (bucket convention (i)); their single-pass 1024-frame medians are given for the budget control.}
\label{tab:s2}
\begin{tabular}{@{}p{0.25\textwidth}p{0.20\textwidth}p{0.21\textwidth}p{0.16\textwidth}p{0.08\textwidth}@{}}
\toprule
Arm & Best-window error (ratio AGC) & Best-window error (log estimator) & Slope, ratio AGC [95\% CI] & Explor.\ slope \\
\midrule
random uniform & --- (bundle $0.007$--$0.017$) & agrees to $\sim2\%$ & $-0.39$ $[-0.42,-0.31]$ & --- \\
random binary & $0.0150$ $[0.0130,0.0165]$ & agrees to $\sim2\%$ & $-0.41$ $[-0.44,-0.37]$ & $-0.39$ \\
SRHT-paired & $0.0077$ $[0.0066,0.0083]$ & agrees to $\sim2\%$ & $-0.58$ $[-0.66,-0.41]$ & $-0.60$ \\
randomly permuted paired Hadamard & $0.0073$ $[0.0063,0.0079]$ & $0.0119$ $[0.0090,0.0133]$ & $-0.62$ $[-0.67,-0.44]$ & $-0.61$ \\
naturally ordered paired Hadamard & --- & shallow decay $-0.222$ $[-0.306,-0.054]$ (slope) & $+0.03$ $[-0.06,+0.17]$ (no decay) & $-0.24$ \\
raw ordered (2048; single-pass $0.896$) & $0.899$ $[0.892,0.903]$, $0.950$ at $W{=}512$ & $0.708$ $[0.563,0.840]$, $\ge0.71$, non-decaying & non-decaying & --- \\
raw shuffled (2048; single-pass $0.788$) & $0.794$ $[0.775,0.816]$ & $0.136$ $[0.083,0.196]$ & --- & --- \\
\bottomrule
\end{tabular}
\end{table*}

\section{Shared simulation protocol: per-figure enumerables}
\label{sec:s5}

Bucket-signal conventions (full statement; main-text Sec.~5): (i) \emph{signed coefficient model} --- $B_n$ is a signed transform coefficient (e.g.\ a bare Walsh--Hadamard coefficient); it can vanish or change sign, so $\log B_n$ is undefined and no log-domain gain estimator is available without further structure; (ii) \emph{physical nonnegative offset model} --- the optical bucket is a nonnegative intensity, with signed patterns realized with a bias/offset so that $B_n\ge B_{\min}>0$; Theorem~B and the main-text Sec.~6 estimators are stated in this model; (iii) \emph{$\pm$ pairwise model} --- each signed pattern is acquired as a complementary pair of nonnegative patterns whose buckets are differenced; the difference is signed again, but the drift enters through adjacent-pair increments controlled by $\rho_{\mathrm{pair}}$ (main-text Sec.~8). The raw signed arms of main-text Sec.~9 follow convention (i) and are diagnostic only.

Notation (consolidated from main-text Sec.~2): $K$ pixels, $N$ frames, drift class $S\ni\mathbf 1$ with $p=\dim S$; gauge $(a,T)\mapsto(c\,a,\,T/c)$; bandwidth $\rho_{\mathrm{bw}}$ ($p\approx\rho_{\mathrm{bw}}N$) versus adjacent-pair decorrelation $\rho_{\mathrm{pair}}$; object functionals $K_{\mathrm{eff}}=S_1^2/S_2$, $K_\infty=S_2/\|T\|_\infty^2$, $K_4=S_2^2/\sum_j T_j^4$; pipeline constants $C_0=\mathbb{E}\|Z\|^2/S_2-1$, $D_H$, and the reconstructor leverage $B_L=(1/S_2)\sum_n b_n^2\|Le_n\|^2$.

All supplementary and main-text experiments share the protocol of Sec.~9 of the main text (matched acquisition budgets, shared OU drift traces within each seed, a single sliding-window-mean AGC estimator for all arms with no per-arm tuning, the ten-object panel spanning $K_{\mathrm{eff}}=59.8$--$864.3$, base seed $20240708$). Per-figure enumerables (main-text figure numbers):

\begin{itemize}[leftmargin=*,itemsep=1pt,topsep=2pt]
\item \textbf{Fig.~2 (carrier stationarity):} 8192 frames per arm at $64\times64$, split into 8 chunks with a 128-frame transient cutoff, AGC window $W=64$, five arms.
\item \textbf{Fig.~3 (blind gain identifiability):} $32\times32$ objects ($K=1024$), 2048 frames per arm, nine arms --- the raw signed-Walsh arms at both the frame-matched 2048 budget (sequence acquired twice) and the single-pass 1024 diagnostic --- $\rho_{\mathrm{pair}}\in\{10^{-3},10^{-2}\}$, drift scale $s=0.1$, $W$ swept over a log-spaced grid from 4 to 1024; every row is scored under both the ratio AGC and the log-domain estimator of Theorem~B; the final uncertainty replication (Table~\ref{tab:s2}) uses 20 drift seeds per (object, $\rho$) pair --- 31{,}600 rows --- while the exploratory $R^2$ analysis used 5 seeds.
\item \textbf{Reconstruction bridge (Fig.~\ref{fig:s2bridge}):} injected $v\in\{0,10^{-4},3\times10^{-4},10^{-3},3\times10^{-3},10^{-2}\}$, $N\in\{1024,2048,4096\}$, 15 seeds, three reconstruction arms.
\item \textbf{Fig.~5 (low-photon robustness):} mean photon budgets $\bar\lambda\in\{0.25,\dots,128\}$ on a $\times2$ grid, soft-log $\alpha=0.5$, $W=64$, $\rho_{\mathrm{pair}}=10^{-3}$, $s=0.1$, 5 seeds, with four estimator arms --- naive clipped log, Anscombe, the uncalibrated soft-log proxy, and the oracle-known-carrier calibrated soft-log of Theorem~C (per-window carrier-calibration root, carriers supplied from the simulation truth as a flat-field-calibrated benchmark; $m_\alpha$ tabulated exactly by truncated Poisson summation, tail bound $\le3.5\times10^{-34}$; the mean-Poisson proxy $m_\alpha^{-1}(\mathrm{mean}_W\,\psi_\alpha)$ retained for continuity) --- compared on the same paired Poisson draw at each budget; all windowed arms use truncated, renormalized windows at the record boundaries (no replicate padding).
\item \textbf{Fig.~6 (threshold scan):} rank/solver scan at $K=64$ (20 seeds, 1300 cells) and $K=128$ (30 seeds, 1950 cells), $p\in\{3,5,9,17,33\}$, margins $N-K-p\in[-8,+16]$ (lower end truncated at $N>K$ for small $p$).
\item \textbf{Pattern conventions:} i.i.d.\ uniform $[0,1]$; i.i.d.\ binary Bernoulli(0.5); paired Hadamard as complementary $(1\pm H)/2$ frames (2 frames per coefficient); SRHT as $H\cdot\mathrm{diag}(\pm1)$ with an optional row permutation, paired the same way; raw signed Walsh arms are diagnostic-only (main-text Sec.~5, bucket convention (i)) and not a valid physical estimator without offset or pairing.
\item \textbf{Metrics:} blind gain error is the scale-aligned relative error (both gain traces normalized to unit mean, optimal scalar alignment); mask-based contrast uses GT-mask CNR at threshold 0.5; reconstruction error is the scale-aligned relMSE of main-text Sec.~8. The OU drift trace is generated with \texttt{normalize\_mean=1}.
\end{itemize}

\clearpage
\appendices
\input{appendix_body}

\input{bibliography_supp}

\end{document}
